import { useState } from 'react'
import { useNavigate, useParams } from 'react-router'
import { Avatar } from '@/core/components/ui/Avatar'
import { BackButton } from '@/core/components/ui/BackButton'
import { Button } from '@/core/components/ui/Button'
import { PageContainer } from '@/core/components/layout/PageContainer'
import { Sheet } from '@/core/components/ui/Sheet'
import { ROUTES } from '@/core/utils/routes'
import { mockConversations } from '@/mocks/data/conversations'
import { mockUsers } from '@/mocks/data/users'
import { removeMockConversation, updateMockConversation } from '../hooks/useConversations'

export default function ChatDetailsPage() {
  const navigate = useNavigate()
  const { conversationId } = useParams()
  const conversation = mockConversations.find((item) => item.id === conversationId) ?? mockConversations[0]
  const contact = mockUsers.find((user) => user.name === conversation.name) ?? mockUsers[0]
  const sharedGroups = mockConversations.filter((item) => item.type === 'group' && item.members.some((member) => member.id === contact.id))
  const [muted, setMuted] = useState(conversation.isMuted)
  const [blocked, setBlocked] = useState(conversation.isBlocked ?? false)
  const [sheet, setSheet] = useState<'report' | 'block' | null>(null)
  const [reportSent, setReportSent] = useState(false)

  return <div className="flex h-full flex-col bg-black text-[#e7e9ea]"><PageContainer className="w-full px-4 pt-3 pb-8"><div className="mx-auto max-w-xl"><BackButton to={`/home/c/${conversation.id}`} /><section className="mt-2 border-b border-[#2f3336] pb-6 text-center"><Avatar src={contact.avatar} alt={contact.name} size="xl" online={contact.isOnline} /><h1 className="mt-3 text-xl font-bold">{contact.name}</h1><p className="mt-1 text-sm text-[#71767b]">{contact.isOnline ? 'متصل الآن' : 'شوهد مؤخراً'}</p><div className="mt-4 flex justify-center gap-2"><Button size="sm" variant="secondary" onClick={() => navigate(`/home/c/${conversation.id}/media`)}>الوسائط المشتركة</Button><Button size="sm" variant="secondary" onClick={() => navigate(`/home/c/${conversation.id}/search`)}>بحث</Button></div></section><section className="mt-6 overflow-hidden rounded-2xl border border-[#2f3336] bg-[#16181c]"><div className="border-b border-[#2f3336] p-4"><p className="text-xs font-bold uppercase tracking-wider text-[#71767b]">المعلومات</p><p className="mt-2 text-sm font-bold">{contact.name}</p><p className="mt-1 text-xs text-[#71767b]">{contact.phone ?? 'لا يوجد رقم هاتف'} · {contact.bio ?? 'لا يوجد نبذة'}</p></div><ActionRow label="كتم الإشعارات" description="إيقاف التنبيهات من هذه المحادثة." value={muted ? 'مفعل' : 'متوقف'} onClick={() => { const next = !muted; setMuted(next); updateMockConversation(conversation.id, { isMuted: next }) }} /><ActionRow label="الوسائط المشتركة" description="الصور والملفات والروابط المتبادلة هنا." value="18 عنصر" onClick={() => navigate(`/home/c/${conversation.id}/media`)} /><ActionRow label="حظر جهة الاتصال" description="إيقاف استقبال الرسائل من جهة الاتصال هذه." value={blocked ? 'محظور' : 'حظر'} danger onClick={() => setSheet('block')} /><ActionRow label="الإبلاغ عن جهة الاتصال" description="إرسال بلاغ أمان لمراجعة المشرف." value="إبلاغ" danger onClick={() => { setReportSent(false); setSheet('report') }} /><ActionRow label="حذف المحادثة" description="إزالة هذه المحادثة من صندوق الوارد." value="حذف" danger onClick={() => { removeMockConversation(conversation.id); navigate(ROUTES.CHAT.LIST) }} /></section><section className="mt-6 overflow-hidden rounded-2xl border border-[#2f3336] bg-[#16181c]"><div className="border-b border-[#2f3336] p-4"><p className="text-xs font-bold uppercase tracking-wider text-[#71767b]">المجموعات المشتركة</p><p className="mt-1 text-xs text-[#71767b]">المجموعات التي تنتميان إليها معاً.</p></div>{sharedGroups.length ? sharedGroups.map((group) => <button type="button" key={group.id} onClick={() => navigate(`/home/g/${group.id}`)} className="flex w-full items-center gap-3 border-b border-[#2f3336] p-4 text-start last:border-b-0 hover:bg-white/[0.03] cursor-pointer"><Avatar src={group.avatar} alt={group.name} size="sm" /><span className="min-w-0 flex-1"><span className="block truncate text-sm font-bold">{group.name}</span><span className="mt-1 block text-xs text-[#71767b]">{group.members.length} أعضاء</span></span><span className="text-xs text-[#1d9bf0]">فتح</span></button>) : <p className="p-4 text-sm text-[#71767b]">لا توجد مجموعات مشتركة بعد.</p>}</section><p className="mt-3 text-center text-xs text-[#71767b]">تنطبق التغييرات على جلسة النموذج الأولي هذه.</p></div></PageContainer><Sheet open={sheet !== null} onClose={() => setSheet(null)} title={sheet === 'block' ? 'حظر جهة الاتصال' : 'الإبلاغ عن جهة الاتصال'}>{sheet === 'block' ? <div><p className="text-sm leading-relaxed text-[#71767b]">جهات الاتصال المحظورة لا يمكنها إرسال رسائل جديدة إليك. يبقى سجل المحادثة الحالي على هذا الجهاز.</p><div className="mt-5 flex gap-2"><Button variant="secondary" className="flex-1" onClick={() => setSheet(null)}>إلغاء</Button><Button variant="danger" className="flex-1" onClick={() => { setBlocked(true); updateMockConversation(conversation.id, { isBlocked: true }); setSheet(null) }}>حظر</Button></div></div> : reportSent ? <div className="py-5 text-center"><p className="font-bold">تم إرسال البلاغ</p><p className="mt-1 text-sm text-[#71767b]">شكراً لمساعدتك في الحفاظ على أمان وصال.</p><Button size="sm" className="mt-4" onClick={() => setSheet(null)}>تم</Button></div> : <div><p className="text-sm text-[#71767b]">اختر سبباً للإبلاغ عن جهة الاتصال هذه.</p><select className="mt-4 h-11 w-full rounded-full border border-[#2f3336] bg-[#202327] px-4 text-sm text-[#e7e9ea] outline-none focus:border-[#1d9bf0]"><option>مضايقة أو إساءة</option><option>رسائل مزعجة</option><option>شيء آخر</option></select><Button className="mt-4 w-full" onClick={() => setReportSent(true)}>إرسال البلاغ</Button></div>}</Sheet></div>
}

function ActionRow({ label, description, value, onClick, danger = false }: { label: string; description: string; value: string; onClick: () => void; danger?: boolean }) {
  return <button type="button" onClick={onClick} className="flex w-full items-center gap-4 border-b border-[#2f3336] p-4 text-start transition-colors last:border-b-0 hover:bg-white/[0.03] cursor-pointer"><span className="min-w-0 flex-1"><span className={`block text-sm font-bold ${danger ? 'text-[#f4212e]' : 'text-[#e7e9ea]'}`}>{label}</span><span className="mt-1 block text-xs text-[#71767b]">{description}</span></span><span className={`shrink-0 text-xs font-bold ${danger ? 'text-[#f4212e]' : 'text-[#71767b]'}`}>{value}</span></button>
}

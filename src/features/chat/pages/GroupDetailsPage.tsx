import { useState } from 'react'
import { useNavigate, useParams } from 'react-router'
import { Avatar } from '@/core/components/ui/Avatar'
import { BackButton } from '@/core/components/ui/BackButton'
import { Button } from '@/core/components/ui/Button'
import { PageContainer } from '@/core/components/layout/PageContainer'
import { Sheet } from '@/core/components/ui/Sheet'
import { mockConversations } from '@/mocks/data/conversations'
import { mockUsers } from '@/mocks/data/users'

export default function GroupDetailsPage() {
  const navigate = useNavigate()
  const { groupId } = useParams()
  const group = mockConversations.find((item) => item.id === groupId) ?? mockConversations.find((item) => item.type === 'group') ?? mockConversations[0]
  const members = group.members.length ? group.members : mockUsers
  const [sheet, setSheet] = useState<'leave' | 'report' | null>(null)
  const [reported, setReported] = useState(false)

  return <div className="flex h-full flex-col bg-black text-[#e7e9ea]"><PageContainer className="w-full px-4 pt-3 pb-8"><div className="mx-auto max-w-2xl"><BackButton to={`/home/g/${group.id}`} /><header className="mt-2 rounded-2xl border border-[#2f3336] bg-[#16181c] p-5"><div className="flex items-center gap-3"><Avatar src={group.avatar} alt={group.name} size="lg" /><div><p className="text-xs font-bold uppercase tracking-wider text-[#1d9bf0]">تفاصيل المجموعة</p><h1 className="mt-1 text-xl font-bold">{group.name}</h1><p className="mt-1 text-sm text-[#71767b]">{members.length} أعضاء · {group.isMuted ? 'مكتومة' : 'التنبيهات مفعلة'}</p></div></div><div className="mt-5 flex flex-wrap gap-2"><Button size="sm" onClick={() => navigate(`/home/g/${group.id}/members`)}>الأعضاء</Button><Button size="sm" variant="secondary" onClick={() => navigate(`/home/g/${group.id}/settings`)}>الإعدادات</Button><Button size="sm" variant="secondary" onClick={() => navigate(`/home/c/${group.id}/media`)}>الوسائط المشتركة</Button></div></header><section className="mt-6 overflow-hidden rounded-2xl border border-[#2f3336] bg-[#16181c]"><div className="border-b border-[#2f3336] p-4"><p className="text-xs font-bold uppercase tracking-wider text-[#71767b]">الأعضاء والأدوار</p></div>{members.map((member, index) => <div key={member.id} className="flex items-center gap-3 border-b border-[#2f3336] p-4 last:border-b-0"><Avatar src={member.avatar} alt={member.name} size="md" online={member.isOnline} /><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{member.name}</p><p className="mt-0.5 text-xs text-[#71767b]">{index === 0 ? 'مسؤول' : 'عضو'}</p></div></div>)}</section><section className="mt-6 overflow-hidden rounded-2xl border border-[#2f3336] bg-[#16181c]"><button type="button" onClick={() => setSheet('report')} className="flex w-full items-center justify-between border-b border-[#2f3336] p-4 text-start hover:bg-white/[0.03] cursor-pointer"><span><span className="block text-sm font-bold text-[#f4212e]">الإبلاغ عن المجموعة</span><span className="mt-1 block text-xs text-[#71767b]">إرسال هذه المجموعة لمراجعة المشرف.</span></span><span className="text-xs font-bold text-[#f4212e]">إبلاغ</span></button><button type="button" onClick={() => setSheet('leave')} className="flex w-full items-center justify-between p-4 text-start hover:bg-white/[0.03] cursor-pointer"><span><span className="block text-sm font-bold text-[#f4212e]">مغادرة المجموعة</span><span className="mt-1 block text-xs text-[#71767b]">يمكنك الانضمام مجدداً بدعوة جديدة.</span></span><span className="text-xs font-bold text-[#f4212e]">مغادرة</span></button></section></div></PageContainer><Sheet open={sheet !== null} onClose={() => setSheet(null)} title={sheet === 'leave' ? 'مغادرة المجموعة' : 'الإبلاغ عن المجموعة'}>{sheet === 'leave' ? <div><p className="text-sm leading-relaxed text-[#71767b]">هل أنت متأكد من رغبتك في مغادرة {group.name}؟</p><div className="mt-5 flex gap-2"><Button variant="secondary" className="flex-1" onClick={() => setSheet(null)}>إلغاء</Button><Button variant="danger" className="flex-1" onClick={() => navigate('/home')}>مغادرة المجموعة</Button></div></div> : reported ? <div className="py-5 text-center"><p className="font-bold">تم إرسال البلاغ</p><p className="mt-1 text-sm text-[#71767b]">تم إرسال المجموعة للمراجعة.</p><Button size="sm" className="mt-4" onClick={() => setSheet(null)}>تم</Button></div> : <div><p className="text-sm text-[#71767b]">اختر سبباً للإبلاغ عن هذه المجموعة.</p><select className="mt-4 h-11 w-full rounded-full border border-[#2f3336] bg-[#202327] px-4 text-sm text-[#e7e9ea] outline-none focus:border-[#1d9bf0]"><option>مضايقة أو إساءة</option><option>رسائل مزعجة أو مضللة</option><option>شيء آخر</option></select><Button className="mt-4 w-full" onClick={() => { setReported(true) }}>إرسال البلاغ</Button></div>}</Sheet></div>
}
